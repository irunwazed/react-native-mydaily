import React from 'react';
import { View, Text, StyleSheet } from 'react-native';


const MenuBar = () => {

	return(
		<View style={styles.container}>
				<Text>sdfgsdgdsfsd</Text>
		</View>
	);
}

const styles = StyleSheet.create({
	container: {
		position: 'absolute',
		bottom: 0,
		width: '100%',
		backgroundColor: 'red',
		paddingBottom: 30,
		padding: 20,
	},
})

export default MenuBar;